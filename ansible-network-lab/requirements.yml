---
# Ansible collections required for this lab
# Install with: ansible-galaxy collection install -r requirements.yml

collections:
  - name: arista.eos
    version: ">=6.0.0"
  - name: ansible.netcommon
    version: ">=5.0.0"
  - name: ansible.utils
    version: ">=2.0.0"
